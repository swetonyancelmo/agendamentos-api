spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/agendamento_db
    username: admin
    password: adminpassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: tecnico
      password: senha_super_segura_123 # Podemos mudar depois